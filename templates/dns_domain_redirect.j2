{% for c in dnsmasq_domain_redirects %}
# Enable forward lookup of the '{{item.name}}' domain:
server=/{{c.dns.domain}}/{{c.dns.upstream}}#{{c.dns.port}}

{% for rs in c.dns.rev_server_cidrs %}
rev-server={{rs}},{{c.dns.upstream}}#{c.dns.port}}
{% endfor %}
